<?php
/* ============================================================
   SAMPLE — copy this to "secret-config.php" ON THE SERVER ONLY
   (Hostinger hPanel -> File Manager). NEVER commit the real one.

   1. Rename/copy this file to:  secret-config.php
   2. Paste your real Gemini API key (starts with AIzaSy...).
   3. (Optional) change the model.

   Best location: one level ABOVE public_html so it is not
   web-accessible. The proxy (api/gemini.php) checks there too.
   ============================================================ */

$GEMINI_API_KEY = "PASTE_YOUR_AIzaSy_KEY_HERE";
$model = "gemini-1.5-flash";
